# MyClaw Issue #2-#36 Delivery Procedure

Last updated: 2026-07-12 (Asia/Shanghai)

## Objective

Complete GitHub issues #2 through #36 with test-driven vertical slices, parallel execution on every independent frontier, batch-level verification and commits, and immediate GitHub issue updates.

This file is the delivery ledger. It was created before any implementation file. Every sub-agent must update its own issue row and append an Agent Log entry before reporting completion. The coordinating agent owns batch verification, commits, and GitHub comments/closure.

## TDD Contract

The public seams are already agreed by the user through issue #1, each child issue's acceptance criteria, and the canonical testing decisions in `docs/myclaw-runtime-contracts.md` and `docs/myclaw-personal-agent-prd.md`.

- Test only observable behavior at these public seams: CLI/REPL, Conversation Port, Management Port, Runtime Core, Memory Manager, Tool Gateway, Session Store, Model Router, and Provider Adapter.
- Work one vertical slice at a time: add one behavior test, run it and capture the expected failure (RED), add only enough implementation for that behavior, then run it to green (GREEN).
- Prefer real in-process integration through public interfaces. Mock only system boundaries such as provider SDKs, HTTP/DNS, clocks, subprocesses, and fault-injected filesystems.
- Do not test private methods, internal call counts, or implementation layout. Expected values must be literals or contract examples rather than recomputing the implementation.
- Each Agent Log entry must record the RED command/failure, GREEN command/result, changed files, and any residual risk.

## Status Protocol

1. Before editing code, the assigned agent changes only its row from `TODO` to `IN_PROGRESS` and adds its agent name.
2. The agent performs strict red-green cycles and runs the narrow issue suite.
3. Before returning, the agent changes its row to `AGENT_DONE` and appends an Agent Log entry with TDD evidence. Sub-agents do not commit or mutate GitHub issues.
4. The coordinator reviews the diff, runs the batch's narrow tests plus the full quality gate, and changes rows to `VERIFIED`.
5. The coordinator creates one batch commit, records its SHA below, comments on each completed issue with tests and commit evidence, then closes it and changes the row to `CLOSED`.
6. A dependent batch cannot start until every dependency row is `CLOSED`. Independent issues on an open frontier are dispatched to separate agents concurrently, subject only to the four-agent runtime limit; overflow starts immediately in the next parallel wave.

Status legend: `TODO` -> `IN_PROGRESS` -> `AGENT_DONE` -> `VERIFIED` -> `CLOSED`; use `BLOCKED` only with a concrete blocker in the Agent Log.

## Dependency Batches

| Batch | Parallel frontier | Gate |
|---|---|---|
| B01 | #2 | None |
| B02 | #3 | #2 |
| B03 | #4 | #3 |
| B04 | #5 | #4 |
| B05 | #6, #13 | #5 |
| B06 | #7 | #6 |
| B07 | #8 | #7 |
| B08 | #9, #11 | #8 |
| B09 | #10, #12, #15, #21, #22, #27 | #9 and/or #11 as listed below |
| B10 | #14, #16 | #12 / #15 |
| B11 | #17, #20, #25, #28 | #16 / #14 + #27 |
| B12 | #18, #19, #23, #26, #31 | #17 and issue-specific gates |
| B13 | #24, #29 | #23 / #13 + #18 + #28 |
| B14 | #30, #32 | #29 / #11 + #18 + #24 + #26 + #31 |
| B15 | #33 | #30 + #32 |
| B16 | #34 | #19 + #21 + #22 + #33 |
| B17 | #35 | #20 + #34 |
| B18 | #36 | #35 |

## Issue Ledger

| Issue | Ticket | Depends on | Batch | Public test seam | Agent | Status | Commit |
|---|---|---|---|---|---|---|---|
| #2 | T01 Installable Python project | None | B01 | installed `myclaw` CLI and reusable public test fixtures | b01_issue_2 | CLOSED | 7b73e26 |
| #3 | T02 Runtime Contracts | #2 | B02 | frozen schemas and Port/Store/Provider/Tool protocols | b02_issue_3 | CLOSED | f32893f |
| #4 | T03 Agent Home and atomic writes | #3 | B03 | Agent Home initializer, atomic file API, Workspace identity | b03_issue_4 | CLOSED | 2865d46 |
| #5 | T04 Configuration | #4 | B04 | `myclaw`, `myclaw config`, validated/redacted config API | b04_issue_5 | CLOSED | ef73b6a |
| #6 | T05 First streaming turn | #5 | B05 | Conversation Port through REPL and Session Store reload | b05_issue_6 | CLOSED | e18725d |
| #7 | T06 Multi-turn Short-term Memory | #6 | B06 | one REPL session across Conversation Port turns | b06_issue_7 | CLOSED | 9e95271 |
| #8 | T07 Model route fallback | #7 | B07 | Model Router route resolution and startup validation | b07_issue_8 | CLOSED | 4941765 |
| #9 | T08 Retry budget | #8 | B08 | Model Router with fake Provider and fake Clock | b08_issue_9 | CLOSED | 39c2865 |
| #10 | T09 Failed/cancelled turns | #9 | B09 | Conversation Port terminal events and reloadable Session Store | b09_issue_10 | CLOSED | 6b652fa |
| #11 | T10 Session metadata/status | #8 | B08 | Session Store metadata and Management Port `/status` | b08_issue_11 | CLOSED | 39c2865 |
| #12 | T11 Async session title | #9, #11 | B09 | Conversation Port latency/history plus Session Store metadata | b09_issue_12 | CLOSED | 6b652fa |
| #13 | T12 Config/memory views | #5 | B05 | Management Port `/config` and `/memory` | b05_issue_13 | CLOSED | e18725d |
| #14 | T13 Resume session | #12 | B10 | Management Port `/resume` and Session Store recovery | b10_issue_14 | CLOSED | 8b8cc3d |
| #15 | T14 Read-only file tool loop | #9 | B09 | Tool Gateway through full Conversation Port model loop | b09_issue_15 | CLOSED | 6b652fa |
| #16 | T15 Tool result/failure semantics | #15 | B10 | Tool Gateway normalization and Agent Events | b10_issue_16 | CLOSED | 8b8cc3d |
| #17 | T16 Permission request loop | #16 | B11 | Conversation Port permission response and Tool Gateway | b11_issue_17 | CLOSED | b7fcdcc |
| #18 | T17 Safe Workspace writes | #17 | B12 | Tool Gateway write/edit behavior at filesystem boundary | - | TODO | - |
| #19 | T18 Interrupted tool-call repair | #10, #17 | B12 | cancelled Conversation Port turn and reloaded Session Store | - | TODO | - |
| #20 | T19 Tool Artifacts | #16 | B11 | Tool Gateway result plus Session Store/artifact files | b11_issue_20 | CLOSED | b7fcdcc |
| #21 | T20 Anthropic Provider | #9 | B09 | Provider Adapter with fake official SDK | b09_issue_21 | CLOSED | 6b652fa |
| #22 | T21 OpenAI-compatible Provider | #9 | B09 | Provider Adapter with fake official SDK | b09_issue_22_reused | CLOSED | 6b652fa |
| #23 | T22 Shell permission policy | #17 | B12 | Tool Gateway Shell policy/catalog | - | TODO | - |
| #24 | T23 Shell process lifecycle | #23 | B13 | Shell tool subprocess boundary | - | TODO | - |
| #25 | T24 WebSearch | #16 | B11 | Tool Gateway WebSearch with fake search boundary | b11_issue_25 | CLOSED | b7fcdcc |
| #26 | T25 SSRF-resistant WebFetch | #25 | B12 | WebFetch HTTP/DNS/peer boundary through Tool Gateway | - | TODO | - |
| #27 | T26 Conversation Summary | #9, #11 | B09 | Runtime Core/Memory Manager with fake Provider | b09_issue_27 | CLOSED | 6b652fa |
| #28 | T27 Consolidation recovery | #14, #27 | B11 | Session Store/summary journal fault-injection API | coordinator | CLOSED | b7fcdcc |
| #29 | T28 Dream memory update | #13, #18, #28 | B13 | Management Port `/dream` and restricted Memory Manager | - | TODO | - |
| #30 | T29 Periodic Memory Task | #29 | B14 | Memory scheduler with fake Clock and Management Port | - | TODO | - |
| #31 | T30 Scheduled Work persistence | #17 | B12 | Tool Gateway permission plus Scheduled Work Store | - | TODO | - |
| #32 | T31 Scheduled Work execution | #11, #18, #24, #26, #31 | B14 | scheduler through Runtime Core and dedicated Session Store | - | TODO | - |
| #33 | T32 Background coordination | #30, #32 | B15 | Runtime event ordering with foreground/background tasks | - | TODO | - |
| #34 | T33 Runtime shutdown | #19, #21, #22, #33 | B16 | REPL/Runtime lifetime and owned resource boundaries | - | TODO | - |
| #35 | T34 Security/fault review | #20, #34 | B17 | end-to-end public seams with boundary fault injection | - | TODO | - |
| #36 | T35 Cross-platform release | #35 | B18 | built wheel, clean installs, CLI smoke, release evidence | - | TODO | - |

## Quality Gate

Every batch must pass the commands established by #2. Until #2 freezes exact commands, the intended gate is:

```text
pytest
ruff check .
ruff format --check .
mypy src tests
python -m build
```

Issue #36 additionally requires Windows and POSIX evidence, clean-wheel installation, `myclaw`/`myclaw config` smoke tests, traceability for all 48 user stories and required tests, manual acceptance status, real-provider smoke status, and known risks.

## Batch Commit And Issue Log

| Batch | Issues | Verification | Commit | GitHub update | Status |
|---|---|---|---|---|---|
| B01 | #2 | 11 pytest + Ruff lint/format + strict mypy + sdist/wheel + CLI smoke | 7b73e26 | #2 commented and closed | CLOSED |
| B02 | #3 | 66 pytest + Ruff lint/format + strict mypy + sdist/wheel | f32893f | #3 commented and closed | CLOSED |
| B03 | #4 | 77 pytest + Ruff lint/format + strict mypy + sdist/wheel | 2865d46 | #4 commented and closed | CLOSED |
| B04 | #5 | 141 pytest + Ruff lint/format + strict mypy + sdist/wheel + installed CLI smoke | ef73b6a | #5 commented and closed | CLOSED |
| B05 | #6, #13 | 169 pytest + Ruff lint/format + strict mypy + sdist/wheel + combined REPL/management integration | e18725d | #6 and #13 commented and closed | CLOSED |
| B06 | #7 | 173 pytest + Ruff lint/format + strict mypy + sdist/wheel + independent contract review | 9e95271 | #7 commented and closed | CLOSED |
| B07 | #8 | 175 pytest + Ruff lint/format + strict mypy + sdist/wheel + Config/Runtime route regression | 4941765 | #8 commented and closed | CLOSED |
| B08 | #9, #11 | 200 pytest + Ruff lint/format + strict mypy + sdist/wheel + combined retry/status review | 39c2865 | #9 and #11 commented and closed | CLOSED |
| B09 | #10, #12, #15, #21, #22, #27 | 102 B09 public-seam pytest + 279 full pytest + Ruff lint/format + strict mypy + sdist/wheel + independent audits | 6b652fa | all six issues commented and closed | CLOSED |
| B10 | #14, #16 | 79 B10 public-seam pytest + 297 full pytest + Ruff lint/format + strict mypy + sdist/wheel + independent audits | 8b8cc3d | #14 and #16 commented and closed | CLOSED |
| B11 | #17, #20, #25, #28 | 27 B11 public-seam pytest + 325 full pytest + Ruff lint/format + strict mypy + sdist/wheel + independent audits | b7fcdcc | all four issues commented and closed | CLOSED |
| B12 | #18, #19, #23, #26, #31 | - | - | - | TODO |
| B13 | #24, #29 | - | - | - | TODO |
| B14 | #30, #32 | - | - | - | TODO |
| B15 | #33 | - | - | - | TODO |
| B16 | #34 | - | - | - | TODO |
| B17 | #35 | - | - | - | TODO |
| B18 | #36 | - | - | - | TODO |

## Agent Log

Append one row per completed or blocked assignment. Keep evidence concise but reproducible.

| Timestamp | Agent | Issue | RED evidence | GREEN evidence | Files / notes |
|---|---|---|---|---|---|
| 2026-07-12 09:16 +08:00 | b01_issue_2 | #2 | `python -m pytest tests/test_package.py -q` -> `ModuleNotFoundError: myclaw`; `python -m pytest tests/test_cli.py -q` -> console executable absent; fixture slices failed first with missing modules/fixtures, missing provider complete/close, and Tool failure not raised; `ruff format --check .` -> 1 file and `python -m mypy src tests` -> 2 errors. | Offline editable install succeeded; `pytest` -> 11 passed; `ruff check .` -> passed; `ruff format --check .` -> 15 files formatted; `mypy src tests` -> 15 files clean; `python -m build --no-isolation` -> wheel + sdist; offline wheel install and `myclaw --help` -> exit 0. | `.gitignore`, `README.md`, `pyproject.toml`, `src/myclaw/`, `tests/`; residual: host setuptools emits an unrelated deprecated `upload_docs` entry-point warning during no-isolation builds, but artifacts build successfully. |
| 2026-07-12 09:35 +08:00 | contract_audit | #3 | N/A - read-only audit. | N/A - read-only audit. | Checked runtime contracts: UUID4 rule conflicts with the Scheduled Work session example; accepted contract invalidates the whole Scheduled Work file despite implementation-plan tolerance; Management/model result shapes and exact event ordering remain underdefined. |
| 2026-07-12 11:24 +08:00 | b02_issue_3 | #3 | `python -m pytest tests/contract/test_common_contracts.py -q` -> `ModuleNotFoundError: myclaw.contracts`; subsequent vertical slices failed first with missing contract exports, invalid counters/UUID1/status combinations accepted (`DID NOT RAISE`), unimplemented error/artifact branches, and missing terminal validators; `python -m mypy src/myclaw/contracts tests/contract tests/fixtures/provider.py tests/fixtures/tool.py` -> 37 errors including Provider/Tool Protocol incompatibility. | Narrow contract + fake suite -> 42 passed; static Protocol slice -> 23 files clean; `python -m pytest -q` -> 49 passed; `python -m ruff check .` -> passed; `python -m ruff format --check .` -> 36 files formatted; `python -m mypy src tests` -> 36 files clean; `python -m build --no-isolation` -> wheel + sdist. | `src/myclaw/contracts/`, `tests/contract/`, typed provider/Tool fakes and tests, `docs/myclaw-runtime-contracts.md`; corrected the Scheduled Work UUID1 example to normative UUID4. Residual: non-persisted model/management shapes use the smallest documented fields and underdefined event status/action/kind values remain strings; Accepted Scheduled Work whole-file invalidation overrides the stale implementation-plan tolerance; build retains the known host `upload_docs` warning. |
| 2026-07-12 11:30 +08:00 | standards_axis | #3 | N/A - read-only standards review. | N/A - read-only standards review. | Findings: invalid five-token cron accepted; Artifact path and Session title invariants not enforced; background completion can interleave with foreground streaming; possible duplicated usage validation. |
| 2026-07-12 11:48 +08:00 | b02_issue_3 | #3 | Cron: `python -m pytest tests/contract/test_memory_scheduling_contracts.py::test_scheduled_work_rejects_range_invalid_five_field_cron -q` -> `DID NOT RAISE` for `99 99 99 99 99`; artifact path matrix -> 4 failed/3 passed (wrong root, invalid Session ID, raw unsafe character, empty encoded ID accepted); title matrix -> 6 failed (metadata/update accepted empty, unnormalized, and 61-code-point titles); event `-k background` -> 1 failed/1 passed (interleaving accepted, background-only valid). | Cron contract -> 5 passed; artifact/session contracts -> 23 passed; title/session contracts -> 20 passed; event contracts -> 6 passed; `python -m pytest -q` -> 66 passed; `python -m ruff check .` -> passed; `python -m ruff format --check .` -> 36 files formatted; `python -m mypy src tests` -> 36 files clean; `python -m build --no-isolation` -> wheel + sdist. | Added `croniter>=6,<7`; exact artifact path/encoding grammar; normalized 60-code-point Session titles; foreground/background event exclusion. Residual: croniter 6.2.2 lacks typing metadata, isolated with `# type: ignore[import-untyped]`; build retains the known host `upload_docs` warning. |
| 2026-07-12 12:18 +08:00 | b03_issue_4 | #4 | RED command form was `python -m pytest <file>::<node> -q`. Atomic nodes `test_failed_atomic_bytes_replace_preserves_official_state` and `test_atomic_text_replace_writes_exact_utf8_bytes` failed with missing module/export. Agent Home nodes `test_production_agent_home_is_fixed`, `test_first_initialization_creates_only_base_state`, and `test_repeated_initialization_preserves_long_term_memory` failed with missing module, missing `initialize`, and the observed overwrite. Workspace nodes `test_windows_drive_workspace_has_the_accepted_identity_and_slug`, `test_posix_workspace_omits_the_root_from_its_slug`, `test_unc_workspace_has_the_accepted_identity_and_slug`, and `test_native_workspace_is_absolutized_and_lexically_normalized` failed with missing module, `ValueError`, wrong UNC slug, and relative-path `ValueError`. | Each identical narrow node command -> 1 passed after its minimal slice; `python -m pytest tests/test_agent_home.py tests/test_atomic_files.py tests/test_workspace.py -q` -> 11 passed; `python -m pytest -q` -> 77 passed; `python -m ruff check .` -> passed; `python -m ruff format --check .` -> 42 files already formatted; `python -m mypy src tests` -> 42 files clean; `python -m build --no-isolation` -> wheel + sdist. | `src/myclaw/{agent_home,atomic_files,workspace}.py`, matching three public-seam test modules, and this ledger. Residual: parent-directory fsync is intentionally best-effort and unavailable on this Windows host, so POSIX directory-fsync durability remains platform-dependent; no cross-process locking by accepted design; build retains the known host `upload_docs` warning. |
| 2026-07-12 13:03 +08:00 | b04_issue_5 | #5 | Vertical RED commands used `python -m pytest <node> -q`: missing template -> `ModuleNotFoundError: myclaw.config`; typed load -> missing `load`; 35-case schema matrix -> missing `ConfigError`; 13-case route matrix -> missing `resolve_route`; parsed/malformed/schema-invalid views -> missing `view`, uncaught `TOMLDecodeError`, then absent validation error; installed bare/config CLIs -> exit 0 placeholder and `No such command 'config'`; atomic publication fault -> raw `OSError`. | Each node passed after its minimal slice; `python -m pytest tests/test_config.py tests/test_cli.py -q` -> 63 passed; `python -m pytest -q` -> 139 passed; `python -m ruff check .` and `python -m ruff format --check .` -> passed/44 files formatted; `python -m mypy src tests` -> 44 files clean; `python -m build --no-isolation` -> wheel + sdist; isolated offline wheel smokes -> missing 2, config-missing 0, valid 0, config-valid 0, invalid 2, config-invalid 2. | `src/myclaw/{config,cli,atomic_files}.py`, `tests/test_{config,cli}.py`, and this ledger. Residual: no cross-process locking by accepted design; atomic create-without-overwrite depends on same-filesystem hard-link support; template IDs use the accepted full-schema examples `anthropic-default` and `openai-local`; build retains the known host `upload_docs` warning. |
| 2026-07-12 13:19 +08:00 | b04_issue_5 | #5 | Review REDs: `python -m pytest tests/test_config.py::test_config_view_redacts_only_the_api_key_when_its_text_is_reused -q` showed global substitution corrupting the comment, provider table, URL, catalog, and routes; `python -m pytest tests/test_config.py::test_config_view_redacts_multiline_api_key_in_valid_dotted_toml -q` safely failed with `ConfigView leaked a multiline plaintext provider API key` for tomlkit's dotted-key proxy. | Both identical node commands -> passed after targeted structured edits; config/CLI suite -> 65 passed; full pytest -> 141 passed; Ruff lint/format -> passed/44 files; strict mypy -> 44 files clean; offline wheel + sdist build -> passed; isolated installed-wheel overlap/multiline CLI smokes -> exit 0/0 with no secret fragments and preserved unrelated text. | `pyproject.toml`, `src/myclaw/config.py`, `tests/test_config.py`, and this ledger. Added source-preserving `tomlkit>=0.13,<0.14` (verified 0.13.3) while retaining `tomllib` for accepted schema validation; installed wheel metadata verified the dependency. Residual: build retains the known host `upload_docs` warning. |
| 2026-07-12 13:51 +08:00 | b05_issue_13 | #13 | Public-seam nodes failed in sequence with missing `myclaw.management`, absent `memory_view`, raw decode/read exceptions, missing `myclaw.management_commands`, `/memory` and unknown slash `NotImplementedError`, omitted config diagnostics, and escaped persistence errors. | Each identical node passed after its minimal slice; `python -m pytest tests/test_management_views.py tests/test_management_commands.py -q` -> 17 passed; config + management suite -> 74 passed; management contract suite -> 4 passed; owned Ruff lint/format and strict mypy -> clean; combined B05 `pytest -q` -> 165 passed, format -> 54 files, offline wheel + sdist -> passed. | `src/myclaw/{management,management_commands}.py`, `tests/test_management_{views,commands}.py`, and this ledger. Complete fresh disk views, stable persistence errors, exact dispatch, and conversation/provider bypass are covered; no CLI/session/runtime/config edits. Residual: combined Ruff import-order and mypy failures were exclusively in concurrently unfinished #6 files and were handed to that agent for the authoritative B05 rerun; build retains the known host `upload_docs` warning. |
| 2026-07-12 14:02 +08:00 | b05_issue_6 | #6 | Public-seam slices failed first with missing `myclaw.session_store`, unsupported assistant reload, missing `myclaw.conversation`, missing REPL/runtime modules, nonblank REPL `NotImplementedError`, absent management hook, absent console input/writer and fail-closed provider factory; first full static gate then exposed 2 format files and 1 callback typing error. | Each identical node passed after its minimal implementation; owned Session Store/Conversation/REPL/runtime/CLI suite -> 19 passed; `python -m pytest -q` -> 169 passed; `python -m ruff check .` -> passed; `python -m ruff format --check .` -> 56 files formatted; `python -m mypy src tests` -> 56 files clean; `python -m build --no-isolation` -> wheel + sdist. | `src/myclaw/{cli,conversation,repl,runtime,session_store}.py`, `tests/test_{conversation,repl,runtime,session_store}.py`, and this ledger. Covers lazy exact JSONL, valid reload, progressive ordered deltas, one completed assistant/terminal, whitespace/EOF, structural #13 dispatch hook, and deferred injectable provider factory. Residual: failure/cancellation, history/system context, metadata updates/listing, and real provider adapters remain their assigned later tickets; Windows extended-length paths are internal only; build retains the known host `upload_docs` warning. |
| 2026-07-12 14:33 +08:00 | b06_issue_7 | #7 | `python -m pytest tests/test_conversation.py::test_consecutive_turns_send_raw_short_term_memory_and_wrap_only_current_input -q` -> request 1 contained only raw current input and request 2 omitted history; `python -m pytest tests/test_runtime.py::test_prepared_repl_reuses_one_session_and_its_startup_system_context -q` -> system prompt was empty; `python -m pytest tests/test_repl.py::test_repl_exit_and_quit_ignore_case_and_whitespace_without_materializing_messages -q` -> padded mixed-case `ExIt` entered the Conversation Port; `python -m pytest tests/test_conversation.py::test_conversation_port_rejects_an_overlapping_foreground_submit -q` -> `DID NOT RAISE RuntimeError`. | Each identical node -> 1 passed after its minimal slice; combined Conversation Port/REPL/runtime/Session Store/management suite -> 27 passed; `python -m pytest -q` -> 173 passed; `python -m ruff check .` -> passed; `python -m ruff format --check .` -> 57 files formatted; `python -m mypy src tests` -> 57 files clean; `python -m build --no-isolation` -> wheel + sdist. | `src/myclaw/{conversation,prompts,repl,runtime}.py`, `tests/test_{conversation,repl,runtime}.py`, and this ledger. Covers raw ordered Short-term Memory, current-only Runtime Context, fixed System Prompt composition and startup memory snapshot, exact exit aliases, unknown slash persistence, and one foreground submit. Residual: Tool guidance is empty until the Tool Gateway slice; tool history, compression, routing/retry, failure/cancellation, title/status, and background work remain their assigned tickets; build retains the known host `upload_docs` warning. |
| 2026-07-12 14:40 +08:00 | b06_review | #7 | N/A read-only review; no implementation RED/GREEN cycle. | CodeGraph-first call-path review against `CONTEXT.md`, `docs/myclaw-runtime-contracts.md`, and GitHub #7; `python -m pytest tests/test_conversation.py tests/test_repl.py tests/test_runtime.py -q` -> 13 passed. | Scope: ordered raw Short-term Memory and current-only Runtime Context, startup System Prompt/Workspace/memory snapshot, case-insensitive padded `exit`/`quit`, unknown slash pass-through, and foreground concurrency guard. No blocking findings. Residual for #10: keep the guard active until the cancelled stream emits its terminal outcome or unwinds, and retain an active stream/task handle for `cancel_active_turn`; the current `finally` correctly releases the guard on completion, error, or generator cancellation. |
| 2026-07-12 14:52 +08:00 | b07_issue_8 | #8 | `python -m pytest tests/test_runtime.py::test_prepared_repl_uses_the_chat_model_route -q` -> provider factory observed `anthropic-default` instead of configured `chat-provider`; `python -m pytest tests/test_runtime.py::test_prepared_repl_rejects_an_unusable_default_even_when_chat_is_usable -q` -> `DID NOT RAISE ConfigError`. | Each identical node -> 1 passed after its minimal slice; unknown route plus static-unavailability/default-fallback Config seam regression -> 48 passed; Runtime Core suite -> 5 passed; `python -m pytest -q` -> 175 passed; `python -m ruff check .` -> passed; `python -m ruff format --check .` -> 57 files formatted; `python -m mypy src tests` -> 57 files clean; `python -m build --no-isolation` -> wheel + sdist; `git diff --check` -> passed. | `src/myclaw/runtime.py`, `tests/test_runtime.py`, and this ledger. Runtime startup now fail-fast validates default, then resolves chat through the existing typed `UserConfiguration.resolve_route` seam; configured chat parameters reach the Provider boundary, while the existing missing/static-unusable route cases fall back to default. Residual: dynamic permanent-error fallback and shared retry budget belong to #9; real adapters belong to #21/#22; memory/cron call sites arrive in later tickets; build retains the known host `upload_docs` warning. |
| 2026-07-12 15:23 +08:00 | b08_issue_11 | #11 | `test_appended_messages_update_session_time_and_actual_cumulative_usage` -> stale `updated_at` and zero usage; `test_metadata_update_preserves_message_bytes_and_sets_exact_session_state` -> `NotImplementedError`; ordinary-append fault node -> assistant missing after injected metadata `os.replace` failure; prepared `/status` node -> missing typed status API; nonzero dispatcher node -> `handled=False`; route identity node -> `provider_id` rejected. | Identical narrow nodes passed after each slice; `python -m pytest -q tests/test_session_store.py tests/test_management_views.py tests/test_management_commands.py` -> 25 passed; owned `ruff check`, `ruff format --check`, strict `mypy`, and `git diff --check` -> passed. | `src/myclaw/{session_store,management,management_commands}.py`, `tests/test_{session_store,management_views,management_commands}.py`, and this ledger. Session append/update/snapshot share one per-session lock; ordinary records use complete append before atomic metadata rewrite; actual usage is separate from ceil(UTF-8 bytes/4) estimate; route snapshot forces `provider_id/model`. Residual: injected metadata rewrite failure intentionally leaves the complete appended record with the previous valid metadata, requiring later recovery policy; real runtime/CLI `/status` composition is explicitly deferred to the B08 coordinator after #9 stabilizes. |
| 2026-07-12 15:25 +08:00 | b08_issue_9 | #9 | `test_model_router_caps_one_logical_stream_at_five_provider_attempts` -> import failed for missing `ModelCallError`; backoff node -> sleeps `[0, 0, 0, 0]` instead of `[0.5, 1, 7, 60]`; permanent fallback node re-raised auth failure without constructing default; complete and close nodes -> `NotImplementedError`; prepared REPL retry node -> unexpected `retry_clock`. | Identical nodes passed after each minimal slice; Model Router suite -> 13 passed; owned Router/Runtime/Conversation/REPL suite -> 29 passed; combined B08 `python -m pytest -q` -> 195 passed; Ruff lint/format -> passed/59 files; strict mypy -> 59 files clean; sdist/wheel build and `git diff --check` -> passed. | `src/myclaw/model_router.py`, typed `ModelCallError` contract/export, `src/myclaw/runtime.py`, `tests/test_model_router.py`, `tests/test_runtime.py`, and this ledger. Covers five-attempt stream/complete budget shared across requested/default, global exponential backoff and capped retry-after, permanent availability fallback, terminal-error/cancellation no-retry, no retry after an observable delta, lazy provider caching/close, and actual REPL routing. Model-not-found/unsupported adapters must normalize to `route_unavailable`; real adapters remain #21/#22 and failed/cancelled turn persistence remains #10; build retains the known host `upload_docs` warning. |
| 2026-07-12 15:30 +08:00 | b08_issue_9 | #9 | Follow-up composition RED: `python -m pytest tests/test_model_router.py::test_model_router_route_status_updates_when_dynamic_fallback_is_selected -q` -> import failed because `ModelRouteStatus` was absent. | Identical node -> 1 passed; static default-status regression -> 1 passed; full Model Router suite -> 15 passed; owned Ruff lint/format and strict mypy -> clean. | Added frozen, secret-free `ModelRouteStatus` and `ModelRouter.route_status()` for `/status` composition: it starts from static resolution without constructing an adapter and updates the logical route snapshot only when dynamic permanent fallback is selected. No Management Port wiring was added; the B08 coordinator owns adaptation to `ResolvedChatStatus` and the final post-composition full gate. |
| 2026-07-12 15:48 +08:00 | b08_issue_9 | #9 | Recovery RED: after a first chat call permanently failed and selected default, a second independent chat call succeeded through the configured chat Provider (`chat=2`, `default=1` requests), but `route_status("chat")` still reported all concrete default fields. | Identical stream recovery node -> 1 passed after the minimal fix; symmetric complete recovery node -> 1 passed; `python -m pytest tests/test_model_router.py tests/test_runtime.py -q` -> 24 passed; related Ruff lint/format, strict mypy, and `git diff --check` -> passed. | `src/myclaw/model_router.py`, `tests/test_model_router.py`, and this ledger. Each stream/complete logical call now refreshes status immediately after successful static route resolution; transient retries retain that selection, dynamic permanent fallback updates it to default, and a later independent call can recover it to the requested configured route. No #11-owned file or composition wiring changed. |
| 2026-07-12 15:49 +08:00 | b08_review | #9, #11 | N/A - read-only review; no implementation RED/GREEN cycle. Independent two-call fake-Provider reproduction initially exposed stale default `route_status("chat")` after the requested route recovered; returned to `b08_issue_9` for its recorded RED/GREEN fix. | CodeGraph-first review against `CONTEXT.md`, runtime contracts 3.3/5/9/14, and GitHub #9/#11; post-fix stream/complete recovery plus composed fallback `/status` nodes -> 4 passed; `python -m pytest -q tests/test_model_router.py tests/test_session_store.py tests/test_management_views.py tests/test_management_commands.py tests/test_runtime.py` -> 49 passed; related `ruff check` and `git diff --check` -> passed. | No remaining B08 blocker. Verified shared five-attempt budget, bounded injectable jitter/retry-after, terminal classifications, no replay after observable stream output, cancellation and adapter close seam; serialized ordinary append plus byte-preserving atomic metadata rewrite; actual usage/timestamps/cursor; secret-free dynamic route status, UTF-8 estimate, and management/provider bypass. Residual: a metadata-rewrite fault may leave a complete appended record with prior metadata for #35 recovery policy; failed/cancelled turn persistence is #10, tool-definition estimation arrives with Tool Gateway, real error normalization with #21/#22, and coordinated resource shutdown with #34. |
| 2026-07-12 15:52 +08:00 | coordinator | #9, #11 | `python -m pytest tests/test_runtime.py::test_prepared_repl_status_reports_the_actual_fallback_route_and_session -q` -> `/status` entered the Conversation Port as a second turn and failed before producing Management output. | Identical node -> 1 passed after runtime composition; B08 public-seam suite -> 57 passed; final `python -m pytest -q` -> 200 passed; Ruff lint/format -> passed/59 files; strict mypy -> 59 files clean; `python -m build --no-isolation` -> wheel + sdist; `git diff --check` -> passed. | `src/myclaw/{cli,prompts,runtime}.py` and `tests/test_runtime.py`. Prepared Runtime now owns a default Management dispatcher, derives a secret-free live chat route from Model Router, estimates the next provider-neutral input from System Prompt/retained messages/Runtime Context, and reports dynamic fallback plus current Session state without another provider call. Explicit test dispatchers still override the default. Build retains the known host `upload_docs` warning. |
| 2026-07-12 16:19 +08:00 | b09_issue_10 | #10 | `failed/interrupted` reload -> `ValueError: Only completed assistant records are supported`; final `ModelCallError` escaped the Port; next request retained a pure error as an empty assistant; `cancel_active_turn` -> `NotImplementedError`; typed empty cancellation was misclassified as failed; writer-injected Ctrl+C -> escaped `CancelledError`; streamed failure lost partial content; completion-less stream -> raw `ValueError`. | Each identical node passed after its vertical fix; `python -m pytest tests/test_conversation.py tests/test_repl.py tests/test_session_store.py -q` -> 24 passed; related contract/router/runtime regression -> 74 passed; owned Ruff lint/format -> clean; strict mypy on 6 owned files -> clean; `git diff --check` -> passed. | `src/myclaw/{conversation,repl,session_store}.py`, `tests/test_{conversation,repl,session_store}.py`, and this ledger. Covers safe final failure projection, exactly one terminal with stable IDs/turn ID, partial and empty cancellation, external active-task cancellation without early guard release, interrupted-history marker, raw user history, and Ctrl+C REPL continuation. Full B09 gate is deferred until concurrent Provider work stabilizes. Residual: coordinated shutdown ownership, repeated shutdown cancellation, and closing/awaiting Provider/background resources belong to #34; B09 coordinator owns aligning `/status` next-request estimation with pure-error omission and interrupted projection. |
| 2026-07-12 16:24 +08:00 | b09_issue_21 | #21 | Vertical REDs: provider module import missing; mixed stream completed without tool calls; `complete` raised `NotImplementedError`; official timeout, rate-limit/retry-after, auth, connection/529, 404, invalid/context/unsupported and generic SDK errors escaped unnormalized; stream creation/iteration errors escaped; malformed streamed/full tool arguments leaked `JSONDecodeError`/`ValueError`; 529 `Retry-After: 4.25` was lost as `None`. | Each identical narrow selection passed after its minimal slice; `python -m pytest -q tests/test_anthropic_provider.py` -> 21 passed; latest full `python -m pytest -q` -> 251 passed; owned Ruff lint/format and strict mypy -> clean; full Ruff lint, strict mypy (64 files), sdist/wheel build, and `git diff --check` -> passed. | Added `src/myclaw/providers/{__init__,anthropic}.py`, `tests/test_anthropic_provider.py`, and this ledger. Fake official SDK boundary covers config-driven zero-retry client, exact request/history/tool conversion, streaming text/mixed tool JSON/usage aggregation, complete/close, safe exact error mapping including retry-after on retryable status failures, malformed output, and cancellation propagation; unsupported `reasoning_effort` is omitted. Residual: latest full format check was blocked only by concurrently edited `tests/test_runtime.py`; coordinator owns that file plus Anthropic dependency, production factory, and package exports; live credential smoke remains a release task; build retains the known host `upload_docs` warning. |
| 2026-07-12 16:31 +08:00 | b09_issue_22_reused | #22 | Vertical REDs: provider module collection failed; mixed stream dropped indexed tool calls; `complete` raised `NotImplementedError`; SDK auth/rate/timeout/temporary/permanent failures and stream create/iteration errors escaped; malformed/non-object tool arguments leaked parser errors; 529 `Retry-After: 4.25` normalized to `None`. | Each identical narrow selection passed after its minimal slice; `python -m pytest -q tests/test_openai_compatible_provider.py` -> 20 passed; final `python -m pytest -q` -> 251 passed; full Ruff lint and strict mypy (64 files), owned Ruff format, sdist/wheel build, and `git diff --check` -> passed. | Added `src/myclaw/providers/openai_compatible.py`, `tests/test_openai_compatible_provider.py`, and this ledger. Injected fake official OpenAI client freezes custom base URL, `max_retries=0`, per-request timeout, OpenAI message/function-tool shapes, indexed streaming text/tool aggregation, memory/cron completion, usage, safe exact `ModelCallError` mapping with retry-after, malformed-output normalization, cancellation, single-call behavior, and close. Residual: full format check reports only coordinator-owned `tests/test_runtime.py`; coordinator owns OpenAI dependency, production factory/exports, and live credential smoke; build retains the known host `upload_docs` warning. |
| 2026-07-12 17:12 +08:00 | b09_issue_15 | #15 | Vertical REDs through `StreamingConversationPort.submit`: list slice failed collection with `ModuleNotFoundError: myclaw.tool_gateway`; read and search slices returned stable `tool_not_found` instead of file results; production slice emitted `[turn_started, turn_completed]` because `prepare_repl_runtime` omitted the Gateway; binary read exposed `prefix\\x00private-payload` instead of an error. | Each identical node passed after its minimal slice; `python -m pytest tests/test_readonly_tool_loop.py -q` -> 7 passed; Conversation/Session/Runtime/title related regression -> 38 passed; focused Ruff lint and format, strict mypy on 6 files, and `git diff --check` -> passed. | Added `src/myclaw/{file_tools,tool_gateway}.py` and `tests/test_readonly_tool_loop.py`; integrated tool definitions/execution/history in `conversation.py`, assistant/tool reload in `session_store.py`, and production/status composition in `runtime.py`. Covers stable Workspace-relative list/search, strict UTF-8 line reads, binary errors, `..` and real Windows junction denial, multiple calls and rounds, exact call-ID persistence/reload, cumulative usage, and final-only loop termination. Residual: broad failure/event semantics are #16, confirmation/write policy #17/#18, interrupted unfinished calls #19, artifacts #20, and POSIX release evidence #36. |
| 2026-07-12 17:14 +08:00 | b09_issue_27 | #27 | Vertical REDs: missing summary module; concurrent auxiliary usage was overwritten (`37 != 47`); token trigger left cursor `0`; repeated summaries wrote indexes `[1,1]`; summary path fault leaked `PermissionError`; oversized system/current-only context still called Provider; Runtime made zero memory calls; empty summary was misclassified `persistence_error`. | Each identical narrow node passed after its minimal slice; `python -m pytest tests/test_conversation_summary.py -q` -> 11 passed; broader Conversation/Session/Router/Runtime/title/tool/summary regression -> 61 passed; related composition suite -> 28 passed; focused Ruff lint/format, strict mypy on 3 files, CodeGraph review, and `git diff --check` -> passed. | Added `src/myclaw/conversation_summary.py` and `tests/test_conversation_summary.py`; composed the memory route and per-chat-call history preparer in `runtime.py`. Covers deterministic message/token triggers, half-budget/threshold selection, forward/fallback user cutoff with current chain retention, no Long-term Memory/tools in summary, no summary injection into chat, ordered JSONL append, atomic usage delta plus cursor, safe persistence/model/context failures, and default-route fallback. Cross-file journal/recovery remains explicitly #28. |
| 2026-07-12 17:13 +08:00 | b09_issue_12 | #12 | Vertical REDs: async title seam rejected `title_prompt`; failed title left `Untitled session` instead of the normalized 60-code-point fallback; `MetadataUpdate` rejected atomic `usage_delta`; completed title usage remained at one chat call; a tool-bearing title response was accepted; prepared runtime issued only the main request; production title consumed the foreground retry fake and raised `No scripted stream remains`. | Each identical node passed after its minimal GREEN; `python -m pytest tests/test_session_title.py tests/test_runtime_session_title.py -q` -> 7 passed; Conversation/Store/tool/contract regression -> 43 passed; full Runtime plus production title regression -> 10 passed; focused Ruff lint/format, strict mypy on 9 files, and `git diff --check` -> passed. | Added `src/myclaw/session_titles.py`, `tests/test_{session_title,runtime_session_title}.py`; integrated a dedicated prompt and captured-session background chat stream in `conversation.py`/`runtime.py`; added lock-atomic `MetadataUpdate.usage_delta` in contracts/Session Store; made the scripted Provider fake isolate unconfigured title requests. Covers nonblocking/no-history title calls, quote/whitespace/Unicode normalization, fallback/Untitled/illegal output, usage, late completion identity, production routing, and consumed background exceptions. Residual: #14 must preserve captured session identity when adding resume switching; coordinated cancellation/await of background title work and Provider close remain #34; live credential smoke remains release work. |
| 2026-07-12 17:22 +08:00 | b09_issue_12_audit | #12 | N/A - read-only audit; cancellation reproduction after first-user persistence observed `{'persisted_messages': 1, 'title_task_created': False, 'provider_calls': 0}`. | N/A - read-only audit; title/runtime/summary concurrency slice -> 8 passed; focused Ruff and mypy passed. | P2 finding: `conversation.py` awaits Session Store reload after persisting the first user but before owning the title task, so cancellation or a suspending/failing Store seam in that window can permanently skip title generation; coordinator accepted for TDD fix. All other reviewed title normalization, isolated streaming chat route, no-history/tools/LTM request, lock-atomic usage delta, captured session identity, foreground UUID/event isolation, and task exception consumption paths matched the accepted contracts. |
| 2026-07-12 17:23 +08:00 | b09_issue_27_audit | #27 | N/A - read-only audit. | N/A - read-only audit; relevant pytest -> 60 passed; focused Ruff, strict mypy, and `git diff --check` passed. | Finding returned for coordinator TDD: a completed summary response's actual usage is lost when content is empty or summary/session persistence fails, contrary to the cumulative-usage contract; existing tests encode the wrong zero-delta expectation. Verified per-tool-loop-call preparation, exact `>=` triggers, full input estimate, forward/fallback user cutoff, current tool-chain retention, memory/default routing, no Long-term Memory/tools/history pollution, no chat summary injection, schema/index, and lock-atomic successful usage delta. Cross-file orphan-summary/crash recovery remains explicitly #28 and is non-blocking for this audit. |
| 2026-07-12 17:22 +08:00 | b09_issue_15_audit | #15 | N/A - read-only CodeGraph-first contract audit. Stdin public-seam reproductions observed: an overlong unknown tool name raised `ValueError: summary must not exceed 240 characters` after only `turn_started`, leaving persisted roles `[user, assistant]`; cancellation during a blocking tool left `[user, assistant, assistant]` with one correlated call but no tool result; `search_files` returned `success` with `binary.dat:1:needle\\x00private`. | N/A - read-only audit; `python -m pytest tests/test_readonly_tool_loop.py tests/test_conversation.py tests/test_runtime.py tests/test_session_store.py -q` -> 32 passed; focused Ruff and strict mypy passed; `git diff --check` reported only existing line-ending warnings. | Findings: production `chat_system_prompt` still receives no Tool Catalog guidance and search does not reject NUL binary content; coordinator accepted both for B09 TDD fixes. The overlong event-summary failure is assigned to #16, and unfinished-call cancellation/correlation recovery to #19. Existing `..` and Windows junction denial, resolved-path containment, stable ordering, mixed assistant content/calls, multiple calls/rounds, exact IDs, reload, cumulative usage, terminal success, structured tool definitions, and status definition estimation otherwise matched the reviewed contracts. |
| 2026-07-12 17:30 +08:00 | coordinator | #10, #12, #15, #21, #22, #27 | Factory slice: `tests/test_provider_factory.py` initially failed on missing exports/factory; CLI integration then exposed the orphaned `try:` syntax error. Status projection REDs observed pure-error and interrupted estimates `139 != 151` and `158 != 166`. Audit follow-up RED command over five nodes -> 4 failed/1 passed for title cancellation before task ownership, summary usage loss on empty/persistence failure, and empty Tool guidance; corrected binary fixture then independently failed with `binary.txt:1:needle\\x00private`. | Factory/CLI -> 10 passed; projection nodes passed through shared `model_message_from_session`; identical audit nodes -> 5 passed after immediate title-task ownership, pre-persistence summary usage delta, Tool Catalog guidance, and NUL-binary search skip. Final B09 suite -> 102 passed; full pytest -> 279 passed; Ruff lint/format -> 73 files clean; strict mypy -> 73 files clean; sdist/wheel build and `git diff --check` passed. | Implementation commit `6b652fa`; GitHub #10/#12/#15/#21/#22/#27 commented and closed. Independent audit otherwise accepted Provider adapters, title isolation, Workspace containment/tool correlation, and summary cutoff/routing. Residuals remain deliberately dependency-owned: bounded/normalized unknown-tool activity and failure semantics in #16, unfinished tool-call cancellation repair in #19, summary journal recovery in #28, title/background/provider ownership in #34, and live provider/cross-platform evidence in #36. Build retains the known host `upload_docs` warning. |
| 2026-07-12 17:57 +08:00 | b10_issue_16 | #16 | Long unknown Tool name node -> `ValueError: summary must not exceed 240 characters` after assistant persistence and before any Tool Result/terminal; invalid-arguments node first rejected the absent injectable catalog, then observed the external Tool execute despite a schema mismatch; execution-failure node leaked raw `RuntimeError: private-service-response-body`; owned format check requested 3 files and strict mypy reported missing `jsonschema` stubs. | Each identical behavior node passed after its minimal slice; `python -m pytest tests/test_tool_failure_semantics.py -q` -> 4 passed; related Conversation/Gateway/event/tool contract regression -> 36 passed; owned Ruff lint/format, strict mypy, and `git diff --check` passed. | `pyproject.toml`, `src/myclaw/{conversation,tool_gateway}.py`, `tests/test_tool_failure_semantics.py`, and this ledger. Gateway now validates declared JSON Schema before the external Tool boundary, executes valid calls once, normalizes all ordinary exceptions without raw details, and supports injected Tool catalogs; activity summaries collapse whitespace/truncate to 240 while preserving exact call/name correlation. Covers mixed assistant content/tool calls, unknown/invalid/exception results, paired activity ordering, multiple mixed outcomes, normalized provider history, reload, usage, and one terminal. Residual: `asyncio.CancelledError` intentionally remains unnormalized here because unfinished-call cancellation repair belongs to #19. Repository-wide static gates were temporarily blocked only by concurrently unfinished #14 files (`session_store.py`/`test_session_resume.py`); #16-owned gates are clean. |
| 2026-07-12 18:05 +08:00 | b10_issue_16_audit | #16 | N/A - read-only CodeGraph-first audit. Public Tool Gateway reproduction declared `address: {type: string, format: email}` and passed `not-an-email`; observed `status=success`, `content=sent`, and `execute_calls=1`, proving Draft 2020-12 `format` annotations are not enforced without a format checker. | N/A - read-only audit; #16/session/provider projection suite -> 90 passed; focused Ruff lint/format, strict mypy, and `git diff --check` passed. Full-tree static checks were separately blocked only by concurrently unfinished #14 files. | Finding returned to coordinator for TDD: `src/myclaw/tool_gateway.py:49` must enforce declared JSON Schema formats and reject before execution with stable `tool_invalid_arguments`. Verified bool-vs-integer and unknown-field rejection, safe unknown/invalid/exception results, execute-once behavior, normalized <=240 summaries without argument/result leakage, mixed content/call ordering and correlation, reload/provider projection, cumulative usage, model continuation, and terminal semantics. Cancellation of unfinished calls remains #19 and was not treated as a blocker. |
| 2026-07-12 18:07 +08:00 | b10_issue_16 | #16 | `python -m pytest tests/test_tool_failure_semantics.py::test_json_schema_format_is_enforced_before_tool_boundary_and_turn_continues -q` -> invalid email passed validation and the external FakeTool executed once (`tool.calls` was non-empty). | Identical node -> 1 passed after enabling the standard JSON Schema format checker; owned #16 suite -> 5 passed; related Conversation/Gateway/event/tool contract regression -> 37 passed; owned Ruff lint/format, strict mypy, and `git diff --check` passed. | `src/myclaw/tool_gateway.py`, `tests/test_tool_failure_semantics.py`, and this ledger. Draft 2020-12 `format` declarations are now enforced before execution and remain normalized to safe `tool_invalid_arguments`; the model still receives the normalized Tool message and completes the turn. Repository-wide static checks remained blocked only by concurrently unfinished #14/audit files; no #16-owned static errors. |
| 2026-07-12 18:13 +08:00 | b10_issue_14 | #14 | Vertical REDs: Workspace picker -> `JsonlSessionStore.list_for_workspace` `NotImplementedError`; corrupt metadata escaped `ValueError`; truncated final append reload rejected `Session must contain complete JSONL records`; `/resume` entered Conversation Port and exhausted the scripted stream; unsupported schema/message record types were listed; aggregate warning used `Session(s)`; post-resume `runtime.session_id` remained the startup ID. | Each identical node passed after its minimal slice; `python -m pytest tests/test_session_resume.py -q` -> 11 passed; related Store/title/Management/REPL/runtime/summary/read-only Tool/#16 regression -> 84 passed; owned Ruff lint/format passed; `python -m mypy src tests` -> 76 files clean; `git diff --check` passed; final CodeGraph call-path review found no blocker. | `src/myclaw/{session_store,session_resume,management,management_commands,repl,runtime}.py`, `tests/test_session_resume.py`, and this ledger. Covers current-Workspace stable summaries, corrupt-session aggregate warning without deletion, strict schema/intermediate/complete-tail rejection, delayed atomic incomplete-tail repair and fault preservation, picker selection/revalidation, target history/request/status/write ownership, dynamic Tool context, nonempty preservation/empty non-materialization, and captured late title identity without retitling resumed history. Residual: picker uses the existing REPL input boundary rather than terminal-specific arrow-key navigation; cross-process coordination remains explicitly out of scope. |
| 2026-07-12 18:23 +08:00 | b10_issue_14_audit | #14 | N/A - read-only CodeGraph-first audit. Public `ConversationPort` reproduction started a blocking Session A turn, switched to B, then started B concurrently; observed `both_active=True`, `cancel_a=0`, `cancel_b=1`. | N/A - read-only audit; `python -m pytest tests/test_session_resume.py -q` -> 11 passed; scoped Ruff lint/format and strict mypy passed; `git diff --check` passed. | P1: `SwitchableConversationPort` can discard an active delegate on resume, admit a second foreground turn, and misroute cancellation/permission, violating the single-foreground Port contract. P2: shared `last_skipped_count` races across concurrent picker listings and can misreport the aggregate warning. TDD concern: the atomic-repair failure test monkeypatches an internal helper instead of a filesystem/atomic-write boundary. Other reviewed Workspace isolation/revalidation, deterministic summaries, strict tail recovery/nondeletion, target history/context/status/tool ownership, old/new Session persistence, and captured late-title identity matched #14. |
| 2026-07-12 18:40 +08:00 | b10_issue_14 | #14 | Audit follow-up REDs: active A allowed `switch_session(B)` and overlapping B (`DID NOT RAISE RuntimeError`), with cancellation routed to B; concurrent `/resume` listings omitted A's corrupt-session warning; `JsonlSessionStore(..., replace_bytes=...)` failed with `unexpected keyword argument 'replace_bytes'`. | Each identical public-seam node passed after its minimal slice; `python -m pytest tests/test_session_resume.py -q` -> 13 passed; related regression -> 86 passed; `python -m ruff check .` and `python -m ruff format --check .` passed; `python -m mypy src tests` -> 76 files clean; `git diff --check` passed; final CodeGraph review found no blocker. | `src/myclaw/{session_resume,session_store,management,management_commands}.py`, `tests/test_session_resume.py`, and this ledger. Active turns now retain delegate ownership and reject switch/overlap until terminal; `SessionListingReport` makes sessions plus skipped diagnostics per-call atomic while preserving `list_for_workspace()`; atomic-repair failure injection uses the typed filesystem boundary. #14 remains `AGENT_DONE` for coordinated B10 commit. |
| 2026-07-12 18:45 +08:00 | coordinator | #14, #16 | #16 audit RED: Draft 2020-12 JSON Schema ignored `format: email`, so an invalid address reached the Tool boundary once. #14 audit REDs: active Session A allowed switching to and overlapping Session B, concurrent listings lost the corrupt-session warning, and the public fault seam initially rejected `replace_bytes`. | #14 owned suite -> 13 passed; #16 owned suite -> 5 passed; final B10 public-seam suite -> 79 passed; full `pytest` -> 297 passed; Ruff lint/format, strict mypy (76 files), sdist/wheel build, `git diff --check`, and CodeGraph review passed. | Implementation commit `8b8cc3d`; GitHub #14/#16 commented and closed. Added active-delegate ownership, per-call listing diagnostics, typed atomic-repair injection, and JSON Schema `FormatChecker`. Unfinished Tool-call cancellation remains deliberately assigned to #19. |
| 2026-07-12 19:03 +08:00 | coordinator | #28 | Vertical REDs: `JsonlSummaryStore(..., replace_bytes=...)` rejected the fault boundary; recovery method was absent; Runtime emitted `turn_started` while the old cursor remained `0`; a pending Session from another Workspace raised raw `FileNotFoundError`; conflicting reserved index leaked raw `ValueError`. | Each identical node passed after its minimal GREEN; four injected crash windows observed exact `journal -> summary -> cursor -> delete` prefixes and recovered to one record/cursor `2`; owned suite -> 9 passed; summary/resume regression -> 33 passed; focused Ruff lint/format, strict mypy, `git diff --check`, and CodeGraph review passed. | `src/myclaw/{conversation_summary,runtime,session_store}.py`, `tests/test_consolidation_recovery.py`, and this ledger. Adds strict six-field journal parsing, atomic/idempotent three-field summary publication, global Session lookup by unique ID, recovery before the first turn event, and safe `persistence_error` normalization. Cross-process index coordination remains explicitly out of scope. |
| 2026-07-12 19:08 +08:00 | b11_issue_20 | #20 | Threshold node -> `ToolGateway.__init__() got an unexpected keyword argument 'max_tool_result_chars'`; Runtime Unicode/unsafe-ID node persisted all `2001` code points with no reference; artifact fault node leaked `RuntimeError: private-artifact-adapter-detail` before a tool result was persisted. | Each identical node passed after its minimal GREEN; `python -m pytest tests/test_tool_artifacts.py -q` -> 3 passed; final Tool/Session/Runtime/permission/WebSearch/recovery regression -> 82 passed; full Ruff lint and format (83 files), strict mypy (83 files), and `git diff --check` passed. | `src/myclaw/tool_artifacts.py`, `src/myclaw/{tool_gateway,runtime}.py`, `tests/test_tool_artifacts.py`, and this ledger. Oversized raw results now use atomic UTF-8 files beside the Session, strict `>` threshold, 2000-code-point previews, canonical unsafe-ID encoding, exact model/Session references, Windows long paths, and safe no-raw failure fallback. Preserved concurrent #17 permission, #25 WebSearch, and #28 recovery composition. Artifact cleanup and cross-process coordination remain out of scope by accepted design. |
| 2026-07-12 19:08 +08:00 | b11_issue_17 | #17 | Vertical REDs: background `write_file` returned `success` and executed; foreground accept and `edit_file` refusal both emitted `tool_completed` instead of `permission_requested`; same-task cancel left the request resolvable; REPL ignored the request and consumed `yes` as a new turn, ending in `async generator raised StopIteration`. | Each identical node passed after its minimal GREEN; `python -m pytest tests/test_permission_loop.py -q` -> 5 passed; permission/Tool/REPL/resume/artifact/web/contract regression -> 64 passed; full `python -m pytest -q` -> 321 passed; focused Ruff lint/format and strict mypy passed; `git diff --check` passed; final CodeGraph call-path review found no blocker. | Added `src/myclaw/permission_policy.py` and `tests/test_permission_loop.py`; integrated validated ASK summaries, approval/refusal, pending-wait cancellation, Session `refused` results, and CLI response handling in `tool_gateway.py`, `conversation.py`, and `repl.py`. Approval resumes the exact call once without permission history; events expose only action/resource/fixed risk. Shared Gateway retains concurrent #20 artifact and #25 web composition. Residual: cancellation materialization of unfinished Tool calls remains explicitly #19. |
| 2026-07-12 19:09 +08:00 | b11_issue_25 | #25 | Vertical REDs: provider-neutral Gateway node failed with `ModuleNotFoundError: myclaw.web_search`; empty-result catalog node rejected `web_search`; DuckDuckGo mapping node could not import `DuckDuckGoSearchBoundary`; Conversation failure node rejected runtime `web_search`; disabled catalog still included `web_search`; enabled production catalog omitted it. | Each identical node passed after its minimal GREEN; `python -m pytest tests/test_web_search.py -q` -> 7 passed; WebSearch/permission/artifact/recovery/Gateway/Conversation/Runtime/config regression -> 102 passed; full `python -m pytest -q` -> 321 passed; `python -m ruff check .`, `python -m ruff format --check .` (83 files), strict `python -m mypy src tests` (83 files), and `git diff --check` passed. | Added `src/myclaw/web_search.py`, `tests/test_web_search.py`, and `ddgs>=9,<10`; additive catalog composition in `src/myclaw/{tool_gateway,runtime}.py` preserves concurrent permission, artifact, and recovery wiring. Covers exact `{title,url,snippet}` JSON through Gateway/Conversation, `[]`, max 1..10/default 5, one safe network failure with no retry/detail leak, SDK field isolation, and config enablement. Residual: live DuckDuckGo network smoke remains a release task; WebFetch remains #26. |
| 2026-07-12 19:16 +08:00 | b11_issue_28_audit | #28 | N/A - read-only CodeGraph-first Standards/Spec audit against GitHub #28, `CONTEXT.md`, runtime contracts 6.3, and ADR 0001. | N/A - read-only audit; consolidation/summary/resume/runtime regression -> 42 passed; focused Ruff lint/format and strict mypy passed; coordinator full gates separately green. | No actionable finding: verified strict six-distinct-field journal and three-field contiguous summary parsing, atomic `journal -> exact summary -> cursor -> delete`, four crash windows, idempotent replay, all-journal loop, cross-Workspace exactly-one Session lookup, recovery before the first event, safe `persistence_error`, and no cross-process guarantee expansion. Residual test gaps: no direct multiple-pending or malformed/duplicate-key journal case; concurrent REPL summary-index contention remains the explicitly accepted ADR risk. |
| 2026-07-12 19:17 +08:00 | b11_issue_17_audit | #17 | N/A - read-only CodeGraph-first Standards/Spec audit. Public `ConversationPort` reproduction consumed through `permission_requested`, closed the event iterator, then observed `stale_resolve=accepted`; one shutdown also reported `RuntimeError: async generator ignored GeneratorExit`. | N/A - read-only audit; permission/resume/REPL/Tool/Event contract regression -> 47 passed; focused Ruff lint/format and strict mypy passed; coordinator full gates separately green. | P1: `conversation.py:183-200` registers the permission Future before yielding, but its cleanup `try/finally` begins only after that yield. Closing or tearing down the standard AsyncIterator at the permission event bypasses removal, so a request from a finished turn remains publicly resolvable and may leak until runtime shutdown. Normal accept/refuse, same-call correlation, no permission history, background refusal, REPL/Switchable routing, explicit cancel happy path, minimal path-only resource/fixed risk, and event order otherwise matched #17. Materializing unfinished calls after cancellation remains explicitly #19. |
| 2026-07-12 19:18 +08:00 | b11_issue_20_audit | #20 | N/A - read-only CodeGraph-first Standards/Spec audit against GitHub #20, `CONTEXT.md`, runtime contracts 1.2/1.4/11.7, and Tool/Session contract tests. Public Gateway reproductions observed empty `tool_call_id` write `.txt` before a raw `ValueError`, Windows `CON` remain `CON.txt` with success, and duplicate IDs share one path whose final content is the later result. | N/A - read-only audit; artifact/Tool/Session/resume regression -> 46 passed; focused Ruff lint/format and strict mypy passed; coordinator full gates separately green. | P1: `tool_artifacts.py:46-72` excludes `ArtifactReference` construction from its guarded write block while `ModelToolCall.id` has no nonempty invariant, so an empty ID writes an unreferenced raw artifact then escapes without a safe tool result. P2: canonical `quote(..., safe="-_.")` leaves Windows device names such as `CON` unchanged; `CON.txt` is reserved, while `ArtifactReference` rejects a noncanonical escape such as `%43ON`, so implementation and persistence contract need a coordinated safe-name rule. Strict `>` threshold, 2000 Unicode-code-point preview, traversal separator encoding, atomic UTF-8 write, no raw fallback on writer failure, exact model/Session preview, and initial/resumed config wiring otherwise matched. Residual: duplicate provider IDs overwrite earlier artifacts because uniqueness is not enforced. |
| 2026-07-12 19:27 +08:00 | coordinator | #17, #20, #25, #28 | Audit follow-up REDs: closing the public permission event iterator left its request resolvable; an empty Tool call ID wrote `.txt` before leaking `ValueError`; Windows device ID `CON` remained reserved `CON.txt`. | Identical nodes passed after explicit nested-generator ownership, pre-write ArtifactReference validation, and shared canonical reserved-name encoding; follow-up permission/artifact/Tool contract suite -> 22 passed; final full `pytest` -> 325 passed; Ruff lint/format (83 files), strict mypy (83 files), sdist/wheel build, `git diff --check`, and final CodeGraph review passed. | Adds synchronous permission-wait teardown on iterator close, safe no-write handling for invalid artifact IDs, and full UTF-8 percent encoding for Windows device basenames with matching persisted contract/docs. #28 independent audit found no blocker; #25 coordinator review found no blocker. Duplicate provider Tool IDs remain a fault-review risk for #35; cross-process summary index coordination remains explicitly out of scope. |
